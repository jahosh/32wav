import { Meteor } from 'meteor/meteor';
import { SimpleSchema } from 'meteor/aldeed:simple-schema';



if (Meteor.isServer) {
  /*
  Meteor.publish('users.info', function userPublication(username){
    return Meteor.users.find({ username: username }, 
      { fields: { "createdAt": 1 } });
  });
  */

 
}